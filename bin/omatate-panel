#!/bin/sh
set -eu
plugin_dir=$(CDPATH= cd -- "$(dirname -- "$(readlink -f -- "$0")")/.." && pwd)
case ${1:-open} in
  open|ping) exec "$plugin_dir/bin/omatate" open ;;
  *) exec "$plugin_dir/bin/omatate" panel "$1" ;;
esac
